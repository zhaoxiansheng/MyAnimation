package com.autopai.common.utils.numeral;

public class Mathematics {
    static public double log(double value, double base) {
        return Math.log(value) / Math.log(base);//logx(y) =loge(y) / loge(x)
    }
}
